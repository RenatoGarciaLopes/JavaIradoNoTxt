package CadastroCliente;

import java.io.IOException;

/**
 * Cadastro de clientes: nome, CPF e e-mail em arquivo TXT (sem criptografia).
 * Responsabilidade única: persistir dados do cliente em TXT.
 */
public class CadastroCliente {

  public void setCaminhoArquivo(String path) {
  }

  /**
   * Salva cliente (nome, CPF, e-mail) em arquivo TXT.
   */
  public void salvar(String nome, String cpf, String email) throws IOException {
  }
}
